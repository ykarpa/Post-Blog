<%- include('./partials/header.ejs') %> <%- include('./partials/nav.ejs') %>

<div class="info1" style="max-width: 520px; margin: 0 auto; border: 10px solid green">
  <% if(posts.length) { %>
  <div>
    <h1 style="text-align: center">All posts</h1>
    <p>
      Lorem ipsum dolor, sit amet consectetur adipisicing elit. Asperiores voluptas quod, ducimus dolorum architecto in,
      laboriosam quos totam soluta fugiat, a illum quibusdam minus.
    </p>
    <table class="table table-striped sorting">
      <thead>
        <tr>
          <th>Title</th>
          <th>Author</th>
          <th>Edit</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        <% if(posts.length) {%> <%posts.forEach((post) => { %>
        <tr id="id_<%= post._id %>" class="">
          <td><%= post.title %></td>
          <td><%= post.author %></td>
          <td><a href="/edit-post/<%= post._id %>">Edit</a></td>
          <td>
            <a class="confirmDeletion" href="" data-id="<%= post._id %>">Delete</a>
          </td>
        </tr>
        <% })}; %>
      </tbody>
    </table>
  </div>
  <%} else{%>
  <h1>There are no posts yet!</h1>
  <%}%>
</div>
<script>
  document.addEventListener("click", (e) => {
    const id = e.target?.dataset?.id || null;
    if (id) {
      fetch(`/posts/${id}`, {
        method: "DELETE",
      }).then(() => {
        window.location.reload();
      });
    }
  });
</script>
<%- include('./partials/footer.ejs') %>

<!-- <%- include('./partials/header.ejs') %> <%- include('./partials/nav.ejs') %>

<style>
  /* Add your styles here */
  body {
    background-color: #f0f0f0;
    color: #333;
    font-family: Arial, sans-serif;
  }

  .main-container {
    max-width: 800px;
    margin: 0 auto;
    background-color: #fff;
    padding: 20px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
  }

  .posts-container {
    margin-top: 20px;
  }

  .post {
    border: 1px solid #ddd;
    padding: 10px;
    margin-bottom: 10px;
    background-color: #fafafa;
  }

  h1,
  h2 {
    color: #333;
  }

  a {
    color: #007bff;
  }
</style>

<div class="main-container">
  <h1><%= title %></h1>

  <div class="posts-container">
    <% if (posts && posts.length > 0) { %> <% posts.forEach(post => { %>
    <div class="post">
      <h2><%= post.title %></h2>
      <p><%= post.author %></p>
    </div>
    <% }); %> <% } else { %>
    <p>No posts available.</p>
    <% } %>
  </div>

  <p>Build with Node & Express.js and MongoDB & Mongoose</p>
</div>

<%- include('./partials/footer.ejs') %> -->
